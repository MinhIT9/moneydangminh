window.http=(()=>{const csrf=document.querySelector('meta[name=csrf-token]').content,getCache=new Map();async function request(path,options={}){const method=(options.method||'GET').toUpperCase(),key=method+':'+path,hit=getCache.get(key);if(method==='GET'&&hit&&Date.now()-hit.time<15000)return hit.data;if(method==='GET'&&hit?.pending)return hit.pending;const run=(async()=>{const opts={...options,headers:{...(options.headers||{}),'X-CSRF-Token':csrf}};if(opts.body&&typeof opts.body!=='string'){opts.headers['Content-Type']='application/json';opts.body=JSON.stringify(opts.body)}const response=await fetch('/api'+path,opts);if(response.status===401){location.href='/login';throw new Error('Bạn cần đăng nhập')}const payload=await response.json().catch(()=>({success:false,message:'Phản hồi không hợp lệ'}));if(!payload.success)throw new Error(payload.message||'Không thể xử lý yêu cầu');if(method==='GET')getCache.set(key,{data:payload.data,time:Date.now()});else{getCache.clear();window.dispatchEvent(new CustomEvent('finance:data-changed',{detail:{path}}))}return payload.data})();if(method==='GET')getCache.set(key,{pending:run,time:0});try{return await run}catch(error){getCache.delete(key);throw error}}return{request,csrf,clearCache:()=>getCache.clear()}})();
