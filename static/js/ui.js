window.ui=(()=>{const modal=new bootstrap.Modal('#formModal'),esc=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),vnParts=()=>Object.fromEntries(new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Ho_Chi_Minh',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date()).filter(x=>x.type!=='literal').map(x=>[x.type,x.value])),today=()=>{const p=vnParts();return`${p.year}-${p.month}-${p.day}`},currentMonth=()=>today().slice(0,7),money=value=>new Intl.NumberFormat('vi-VN').format(Number(value)||0)+' đ',moneyText=value=>{const digits=String(value??'').replace(/\D/g,'').replace(/^0+(?=\d)/,'');return digits?digits.replace(/\B(?=(\d{3})+(?!\d))/g,'.'):''},dateVn=value=>{const m=String(value||'').match(/^(\d{4})-(\d{2})-(\d{2})$/);return m?`${m[3]}/${m[2]}/${m[1]}`:value||''},dateIso=value=>{const m=String(value||'').match(/^(\d{2})\/(\d{2})\/(\d{4})$/);return m?`${m[3]}-${m[2]}-${m[1]}`:value},input=(name,label,type='text',value='',extra='')=>type==='number'?`<label>${esc(label)}</label><div class="vnd-input"><input class="form-control" name="${esc(name)}" type="text" inputmode="numeric" data-money value="${esc(moneyText(value))}" ${extra}></div>`:type==='date'?`<label>${esc(label)}</label><input class="form-control" name="${esc(name)}" type="text" inputmode="numeric" data-date-vn placeholder="ngày/tháng/năm" value="${esc(dateVn(value))}" ${extra}>`:`<label>${esc(label)}</label><input class="form-control" name="${esc(name)}" type="${esc(type)}" value="${esc(value)}" ${extra}>`,select=(name,label,items,value='')=>name==='type'?`<label>${esc(label)}</label><div class="type-segment" role="group">${items.map(x=>`<button type="button" class="type-choice ${String(x.id)===String(value)?'active':''}" data-value="${esc(x.id)}">${x.id==='income'?'<i class="fa-solid fa-arrow-trend-up"></i>':'<i class="fa-solid fa-arrow-trend-down"></i>'}${esc(x.name)}</button>`).join('')}</div><select class="d-none" name="type">${items.map(x=>`<option value="${esc(x.id)}" ${String(x.id)===String(value)?'selected':''}>${esc(x.name)}</option>`).join('')}</select>`:`<label>${esc(label)}</label><select class="form-select" name="${esc(name)}">${items.map(x=>`<option value="${esc(x.id)}" ${String(x.id)===String(value)?'selected':''}>${esc(x.name)}</option>`).join('')}</select>`;function toast(message,bad=false){const node=document.createElement('div');node.className=`toast show text-bg-${bad?'danger':'success'}`;const body=document.createElement('div');body.className='toast-body';body.textContent=message;node.append(body);document.querySelector('#toasts').append(node);setTimeout(()=>node.remove(),3500)}function bindFields(){modalForm.querySelectorAll('[data-money]').forEach(el=>el.oninput=()=>{el.value=moneyText(el.value);el.setSelectionRange(el.value.length,el.value.length)});modalForm.querySelectorAll('[data-date-vn]').forEach(el=>el.oninput=()=>{const digits=el.value.replace(/\D/g,'').slice(0,8);el.value=[digits.slice(0,2),digits.slice(2,4),digits.slice(4,8)].filter(Boolean).join('/')});modalForm.querySelectorAll('.type-choice').forEach(button=>button.onclick=()=>{const selectEl=modalForm.elements.type;selectEl.value=button.dataset.value;modalForm.querySelectorAll('.type-choice').forEach(x=>x.classList.toggle('active',x===button));selectEl.dispatchEvent(new Event('change',{bubbles:true}))})}function values(){const data=Object.fromEntries(new FormData(modalForm));modalForm.querySelectorAll('[data-money]').forEach(el=>data[el.name]=el.value.replace(/\D/g,''));modalForm.querySelectorAll('[data-date-vn]').forEach(el=>data[el.name]=dateIso(el.value));return data}function form(title,html,submit,after){modalTitle.textContent=title;modalBody.innerHTML=html;bindFields();modalForm.onsubmit=async event=>{event.preventDefault();const button=event.submitter;button.disabled=true;try{await submit(values());modal.hide();toast('Đã lưu thành công');if(after)await after()}catch(error){toast(error.message,true)}finally{button.disabled=false}};modal.show()}function confirmAction(title,message,action,after){form(title,`<p>${esc(message)}</p>`,action,after)}return{esc,money,today,currentMonth,dateVn,input,select,toast,form,confirmAction}})();
