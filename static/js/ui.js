window.ui=(()=>{const modal=new bootstrap.Modal('#formModal');const esc=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const money=value=>new Intl.NumberFormat('vi-VN').format(Number(value)||0)+' ₫';const today=()=>new Date().toISOString().slice(0,10);const input=(name,label,type='text',value='',extra='')=>`<label>${esc(label)}</label><input class="form-control" name="${esc(name)}" type="${esc(type)}" value="${esc(value)}" ${extra}>`;const select=(name,label,items,value='')=>`<label>${esc(label)}</label><select class="form-select" name="${esc(name)}">${items.map(x=>`<option value="${esc(x.id)}" ${String(x.id)===String(value)?'selected':''}>${esc(x.name)}</option>`).join('')}</select>`;function toast(message,bad=false){const node=document.createElement('div');node.className=`toast show text-bg-${bad?'danger':'success'}`;const body=document.createElement('div');body.className='toast-body';body.textContent=message;node.append(body);document.querySelector('#toasts').append(node);setTimeout(()=>node.remove(),3500)}function form(title,html,submit,after){modalTitle.textContent=title;modalBody.innerHTML=html;modalForm.onsubmit=async event=>{event.preventDefault();const button=event.submitter;button.disabled=true;try{await submit(Object.fromEntries(new FormData(modalForm)));modal.hide();toast('Đã lưu thành công');if(after)await after()}catch(error){toast(error.message,true)}finally{button.disabled=false}};modal.show()}function confirmAction(title,message,action,after){form(title,`<p>${esc(message)}</p>`,action,after)}return{esc,money,today,input,select,toast,form,confirmAction}})();
